
KVM Software — Dia 1

- Flask é o framework que estou usando no backend.
- Flask(__name__) cria a aplicação Flask.
- __name__ é uma variável especial do Python.
- @app.route("/") define a rota da página inicial.
- def home() é a função executada nessa rota.
- render_template() carrega um template HTML.
- "index.html" é o arquivo que será carregado.
- return devolve o resultado da função.
- A pasta templates guarda os templates HTML.
